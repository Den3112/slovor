#!/usr/bin/env bash

# Pre-start check - removed Docker checks as they don't work inside container
# This script now just confirms the environment is ready

set -e

echo "✓ Pre-start checks passed"
exit 0
